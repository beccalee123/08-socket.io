'use strict';

// const io = require('socket.io')(3000);

// io.on('connection', (socket) => {
//   console.log('New connection', socket.id);
//   socket.on('save', () => {
//     console.log({payload});
//     socket.broadcast.emit('file saved', payload);
//   });
// });